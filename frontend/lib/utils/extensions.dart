extension BoolParsing on String {
  // Function to parse a string into a boolean value
  parseBool() {
    // Convert the string to lowercase and compare it with "true"
    return toLowerCase() == "true";
  }
}
